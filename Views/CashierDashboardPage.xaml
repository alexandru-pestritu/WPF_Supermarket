<Page x:Class="WPF_Supermarket.Views.CashierDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_Supermarket.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CashierDashboardPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="LightGray">
            <TextBlock Text="Cashier Menu" FontSize="20" FontWeight="Bold" Margin="10"/>
            <Button Content="Products" Command="{Binding NavigateToProductsCommand}" Margin="10"/>
            <Button Content="Receipts" Command="{Binding NavigateToReceiptsCommand}" Margin="10"/>
            <Button Content="Logout" Command="{Binding LogoutCommand}" Margin="10"/>
        </StackPanel>

        <Frame x:Name="CashierFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
